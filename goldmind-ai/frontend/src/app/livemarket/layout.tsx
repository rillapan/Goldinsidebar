import DashboardShell from '@/components/DashboardShell';

export default function LiveMarketLayout({ children }: { children: React.ReactNode }) {
  return <DashboardShell>{children}</DashboardShell>;
}
